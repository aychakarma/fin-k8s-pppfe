apiVersion: v1
kind: Service
metadata: 
  name: dev-backend
  labels:
    name: backend-service
spec:
  type: NodePort
  ports:
    - port: 8020
      targetPort: 8020
      nodePort: 30007
  selector:
    app: backend-app
