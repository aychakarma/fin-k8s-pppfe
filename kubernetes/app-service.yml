apiVersion: v1
kind: Service
metadata:
  name: eqprod-backend
  labels:
    name: backend-service
spec:
  type: NodePort
  ports:
    - port: 8030
      targetPort: 8030
      nodePort: 30006
  selector:
    app: backend-app
