apiVersion: v1
kind: Service
metadata: 
  name: prod-backend
  labels:
    name: backend-service
spec:
  type: NodePort
  ports:
    - port: 8040
      targetPort: 8040
      nodePort: 30004
  selector:
    app: backend-app
