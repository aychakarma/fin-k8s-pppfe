apiVersion: v1
kind: Service
metadata: 
  name: prod-backend
  labels:
    name: backend-service
spec:
  type: NodePort
  ports:
    - port: 8060
      targetPort: 8060
      nodePort: 30002
  selector:
    app: backend-app